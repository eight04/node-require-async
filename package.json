{
  "name": "node-require-async",
  "version": "0.1.1",
  "description": "A require function in node, but an asynchronous one.",
  "author": "eight04 <eight04@gmail.com>",
  "license": "MIT",
  "scripts": {
    "lint": "eslint **/*.js --ignore-pattern coverage --ignore-pattern test/fixture --cache",
    "nyc": "nyc --reporter=html mocha",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "test": "npm run lint && npm run nyc",
    "preversion": "npm test",
    "postversion": "git push --follow-tags && npm publish"
  },
  "main": "index.js",
  "files": [],
  "dependencies": {},
  "devDependencies": {
    "coveralls": "^3.0.1",
    "eslint": "^4.19.1",
    "mocha": "^5.2.0",
    "nyc": "^11.8.0",
    "rejected-or-not": "^1.0.0"
  },
  "repository": "eight04/node-require-async"
}
